# 多主播部署指南

每位主播使用独立部署项目、独立 Supabase 数据库和独立域名。代码可以共用此仓库的 main 分支。

## 第一次部署

1. 新建 Supabase 项目，在空数据库中执行 `supabase/schema.sql`。此脚本不是可反复执行的升级迁移。
2. 在 Supabase Auth 中创建受信任的管理员，并关闭公开注册。本项目按单主播设计，不在同一数据库内隔离多位主播。
3. 创建 Vercel 项目并连接此仓库。参照 `.env.example` 填写四个数据库与认证环境变量，为每个站点生成独立的 `AUTH_SECRET`。
4. 配置下面的公开品牌变量，完成部署并绑定这位主播的域名。
5. 登录 `/admin/login`，在页面设置中填写首页主标题、Bilibili 地址，上传头像和背景，再导入该主播自己的歌曲。

不要从其他主播的站点复制 `.env`、数据库内容或管理员凭据。

## 品牌配置

| 环境变量                  | 用途                                           | 默认值                           |
| ------------------------- | ---------------------------------------------- | -------------------------------- |
| `PUBLIC_SITE_TITLE`       | 浏览器标题、导航标题、页脚及未设置的首页主标题 | 主播歌单                         |
| `PUBLIC_SITE_SUBTITLE`    | 导航副标题                                     | 主播歌单与愿望单管理             |
| `PUBLIC_SITE_DESCRIPTION` | 网页描述                                       | 公开歌单、搜索筛选与愿望单提交。 |
| `PUBLIC_SITE_TAGLINE`     | 首页说明                                       | 直播查歌、收歌、管歌，一站完成。 |
| `PUBLIC_SITE_ICON`        | 导航图标及 favicon 的图片 URL                  | 空，导航显示“歌”字               |

图标可使用独立存储空间中的公开 HTTPS 图片地址，也可使用 `static/` 内自有资源的路径，例如 `/brand/icon.png`。推荐使用独立存储，避免所有主播的素材都打包进共用代码。

后台设置的首页主标题优先于环境变量默认标题。修改部署环境变量后重新部署；修改后台资料后刷新页面。

## 可选动态指针

默认使用系统指针，不包含主播指针素材。

设置 `PUBLIC_CUSTOM_CURSORS=true`，并填写全部五个图片 URL 才会启用：

| 环境变量                 | 状态                                     |
| ------------------------ | ---------------------------------------- |
| `PUBLIC_CURSOR_DEFAULT`  | 普通                                     |
| `PUBLIC_CURSOR_POINTER`  | 按钮、链接、歌曲行                       |
| `PUBLIC_CURSOR_TEXT`     | 文本                                     |
| `PUBLIC_CURSOR_DISABLED` | 禁用                                     |
| `PUBLIC_CURSOR_PROGRESS` | 处理中，供 `data-cursor="progress"` 使用 |

图片显示为 32×32，热点在 `src/lib/branding.ts` 定义，须与自有图片对齐。五张图片加载成功后，在鼠标进入页面时启用。加载失败、触控设备或减少动画偏好均保留系统指针。不要为测试而填入未获授权的其他主播素材。

## 后续更新

通用功能在此仓库开发、验证后合并 main，各部署项目更新到该版本。数据库结构变更需要逐个项目应用迁移，不能通过重复运行初始化脚本升级。

原个人站点仓库不会自动同步本仓库。将原站迁入共用代码前，应先在预览部署中配置其独立品牌和数据库并验证功能。

## 本地开发

安装依赖后复制 `.env.example` 为 `.env`，填写新站自己的配置：

```powershell
npm ci
Copy-Item .env.example .env
npm run dev
```

`.env` 已被 Git 忽略。首次验证使用 `npm run check` 和 `npm run build`。

## 来源

基于 QingKong Songlist 及其功能改进整理，保留原 LICENSE 和作者声明。新仓库使用独立初始提交，不携带个人站点的历史图片。
